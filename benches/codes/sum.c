#include <stdio.h>

int main(void) {
    double x1 = 10.0;
    double x2 = 5.0;
    double x3 = x1 + x2;              // compute the sum

    printf("the sum of %.1f and %.1f is %.1f\n", x1, x2, x3);
    return 0;
}
